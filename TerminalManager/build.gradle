plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    compileSdkVersion 34

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 34
    }
}

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId = 'com.github.KOZENTech-Repo'
                artifactId = 'TerminalManagerLib'
                version = '1.3.0'

                artifact("$projectDir/libs/TerminalManagerLib_1.3.0_release.aar")
            }
        }
    }
}
